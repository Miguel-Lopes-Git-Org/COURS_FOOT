version: "3.9"

services:
    web:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: foot-app-prod
        restart: unless-stopped
        ports:
            - "1003:80"
        env_file:
            - WEB/.env
